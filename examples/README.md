# Examples

The workspace root has no package, so worked examples live inside the crates
they demonstrate, e.g.:

- `crates/providers/example/oxisport-example/examples/activity_source.rs`

Cross-provider workflow examples will be added in later milestones, once the
workflow layer exists.
